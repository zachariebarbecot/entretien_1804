package org.lacombe.coffemaker;

public class ProtocolMaker {

    public String generateOrder(DrinkTypes drinkType, int numberOfSugar){
        String order = "";
        char drinkCaracter='T';
        if(drinkType.equals())

        
        return order;
    }
}
